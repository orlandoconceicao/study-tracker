[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py tests.py
testpaths = users studies notifications tests
addopts = --strict-markers --strict-config --tb=short
markers =
    unit: tests that do not cross an HTTP boundary
    integration: tests that exercise API, database, or framework integration
